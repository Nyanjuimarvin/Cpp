#include <stdio.h>
#include <stdlib.h>

int main()
{
    double num,root;
    printf("Enter a number: ");
    scanf("%lf",&num);
    root=sqrt(num);
    printf("%f",root);
    return 0;
}
